\cvsection{Certification}
\BLOCK{ for cert in certifications }
\cvcert{ \VAR{ cert.name } }{ \VAR{ cert.issuer } }{ \VAR{ cert.year | default(cert.date[:4] if cert.date else '') } }
\BLOCK{ if not loop.last }
\divider
\BLOCK{ endif }
\BLOCK{ endfor }